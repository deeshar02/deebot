# Progress

> **Template usage:** Mirror the module list from `PRD.md` here. This file is the
> single source of truth for what's done. It is updated by `/dev:build` and
> `/dev:handover` — keep the marker convention exact so agents can parse it.
>
> **Markers:**
> - `[ ]` not started
> - `[-]` in progress
> - `[x]` complete — the item's check ran and passed (a module is only `[x]` when
>   *every* sub-item is `[x]`)
> - `[!]` blocked — put the reason on the same line

## Module 1: {Name}

- [ ] {Sub-task 1 — small enough to complete and validate in one sitting}
- [ ] {Sub-task 2}
- [ ] {Validation: the module's "Delivers" state is observable}

## Module 2: {Name}

- [ ] {Sub-task 1}
- [ ] {Sub-task 2}

---

## Session Log

<!-- Optional: one line per working session — date, what moved, by whom.
     Keep it terse; details belong in the plan files under docs/plans/. -->

- {YYYY-MM-DD} — Project scaffolded from template.
